{
  "name": "paxi-dex",
  "version": "1.0.0",
  "description": "Canonix",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "express": "^5.2.1",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
